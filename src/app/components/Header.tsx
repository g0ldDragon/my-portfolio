// src/components/Header.tsx

import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="bg-oxford-blue text-white">
      <nav className="container mx-auto py-4">
        ABOUT
        CONTACT
        HOME
      </nav>
    </header>
  );
};

export default Header;
